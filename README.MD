<div align="center">

<img src="https://img.shields.io/badge/-%20SD%20-F5A623?style=for-the-badge&logoColor=000000&labelColor=F5A623" alt="SD Logo" width="60" height="60" />

# SERVICE DESK

**Швидка та зручна система для подачі заявок на технічну підтримку.**  
Створюйте тікети, відслідковуйте їхній статус та отримуйте допомогу від адміністраторів у реальному часі.

[![FastAPI](https://img.shields.io/badge/FastAPI-0d0d0d?style=for-the-badge&logo=fastapi&logoColor=F5A623)](https://fastapi.tiangolo.com)
[![React](https://img.shields.io/badge/React-0d0d0d?style=for-the-badge&logo=react&logoColor=F5A623)](https://react.dev)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-0d0d0d?style=for-the-badge&logo=postgresql&logoColor=F5A623)](https://postgresql.org)
[![Redis](https://img.shields.io/badge/Redis-0d0d0d?style=for-the-badge&logo=redis&logoColor=F5A623)](https://redis.io)
[![Docker](https://img.shields.io/badge/Docker-0d0d0d?style=for-the-badge&logo=docker&logoColor=F5A623)](https://docker.com)

</div>

---

## `>` ОГЛЯД

Service Desk — це fullstack веб-застосунок для керування технічними заявками всередині організації. Користувачі подають заявки, адміністратори беруть їх у роботу, змінюють статуси, додають сервісні записи та спілкуються через систему відповідей. Сповіщення надходять на **email** та **Telegram**.

---

## `>` СТЕК ТЕХНОЛОГІЙ

### Backend
| Технологія | Призначення |
|---|---|
| `FastAPI` | REST API фреймворк |
| `SQLAlchemy` + `asyncpg` | Async ORM + PostgreSQL драйвер |
| `Alembic` | Міграції бази даних |
| `Redis` | Кешування, rate limiting, чорний список токенів |
| `python-jose` | JWT автентифікація |
| `argon2-cffi` | Хешування паролів |
| `fastapi-mail` | Email сповіщення |
| `aiogram` | Telegram бот сповіщення |
| `fastapi-limiter` | Rate limiting |

### Frontend
| Технологія | Призначення |
|---|---|
| `React` + `TypeScript` | UI фреймворк |
| `Vite` | Білд інструмент |

### Інфраструктура
| Технологія | Призначення |
|---|---|
| `Docker` + `Docker Compose` | Контейнеризація |
| `Nginx` | Reverse proxy / static serving |

---

## `>` ФУНКЦІОНАЛ

### 👤 Користувач
- Реєстрація та авторизація через JWT (access + refresh токени)
- Підтвердження email через 6-значний код
- Прив'язка Telegram акаунту для сповіщень
- Керування профілем (зміна username, email, пароля)
- Подача заявок з описом та зображенням
- Перегляд статусу своїх заявок

### 🔑 Адміністратор
- Перегляд усіх заявок у системі
- Призначення себе відповідальним за заявку
- Зміна статусу заявки (`виконано` / `відмовлено`)
- Додавання відповідей до заявок
- Створення сервісних записів (виконані роботи, використані деталі, гарантія)
- Автоматичні email + Telegram сповіщення при зміні статусу

### 🔒 Безпека
- Rate limiting на чутливих ендпоінтах
- Blacklist токенів при видаленні акаунту
- Захищені маршрути (тільки верифіковані / тільки адміни)

---

## `>` API ЕНДПОІНТИ

<details>
<summary><b>👤 Користувачі</b></summary>

| Метод | Шлях | Опис | Доступ |
|---|---|---|---|
| `POST` | `/register` | Реєстрація | Публічний |
| `POST` | `/login` | Авторизація | Публічний |
| `POST` | `/auth/refresh` | Оновлення токена | Публічний |
| `GET` | `/users/me` | Профіль | Авторизований |
| `PATCH` | `/users/me` | Оновити профіль | Авторизований |
| `DELETE` | `/users/me` | Видалити акаунт | Авторизований |
| `POST` | `/verify-email` | Підтвердити email | Авторизований |
| `POST` | `/resend-code` | Відіслати код знову | Авторизований |
| `POST` | `/users/makeadmin` | Стати адміном | Верифікований |

</details>

<details>
<summary><b>📱 Telegram</b></summary>

| Метод | Шлях | Опис | Доступ |
|---|---|---|---|
| `POST` | `/users/telegram/generate-link` | Згенерувати посилання для прив'язки | Верифікований |
| `POST` | `/users/telegram/confirm` | Підтвердити прив'язку (від бота) | Публічний |
| `GET` | `/users/telegram/check/{telegram_id}` | Перевірити прив'язку | Публічний |
| `PATCH` | `/users/telegram/unlink` | Від'єднати Telegram | Верифікований |

</details>

<details>
<summary><b>📋 Заявки</b></summary>

| Метод | Шлях | Опис | Доступ |
|---|---|---|---|
| `POST` | `/problems` | Створити заявку | Верифікований |
| `GET` | `/problems` | Список заявок | Верифікований |
| `GET` | `/problems/{id}` | Деталі заявки | Верифікований |
| `DELETE` | `/problems/{id}` | Видалити заявку | Власник / Адмін |

</details>

<details>
<summary><b>🔑 Адмін</b></summary>

| Метод | Шлях | Опис | Доступ |
|---|---|---|---|
| `PATCH` | `/problems/{id}/status` | Змінити статус | Адмін |
| `PATCH` | `/problems/{id}/assign` | Взяти у роботу | Адмін |
| `POST` | `/problems/response` | Додати відповідь | Адмін |
| `POST` | `/service-record` | Створити сервісний запис | Адмін |

</details>

---

## `>` СТАТУСИ ЗАЯВОК

```
В обробці  ──►  (адмін бере в роботу)  ──►  виконано
                                        └──►  відмовлено
```

---

## `>` ЗАПУСК

### Вимоги
- Docker & Docker Compose
- PostgreSQL
- Redis

### Налаштування `.env`

```env
# Database
DATABASE_URL=postgresql+asyncpg://user:password@db:5432/servicedesk

# Security
SECRET_KEY=your-secret-key-here

# Email
MAIL_USERNAME=your@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM=your@gmail.com

# Telegram Bot
BOT_TOKEN=your-telegram-bot-token

# Redis
REDIS_URL=redis://redis:6379
```

### Docker Compose

```bash
# Клонувати репозиторій
git clone https://github.com/uznikturbo/service.git
cd service

# Запустити
docker-compose up --build
```

Застосунок буде доступний на `http://localhost:80`

### Локальна розробка (без Docker)

```bash
# Backend
cd backend
pip install -r requirements.txt
alembic upgrade head
uvicorn main:app --reload --port 8000

# Frontend
cd frontend
npm install
npm run dev
```

---

## `>` СТРУКТУРА ПРОЄКТУ

```
servicedesk/
├── backend/
│   ├── tests/              # Тести
│   │    ├── __init__.py
│   │    └── tests.py
│   ├── email_templates/    # Шаблони email повідомлень
│   ├── main.py             # FastAPI app, всі ендпоінти
│   ├── models.py           # SQLAlchemy моделі
│   ├── schemas.py          # Pydantic схеми
│   ├── crud.py             # Database операції
│   ├── security.py         # JWT, хешування
│   ├── utils.py            # Допоміжні функції, залежності
│   ├── db.py               # Database підключення
│   ├── redis_config.py     # Redis підключення
│   ├── bot.py              # Telegram бот
│   └── migrations/         # Міграції
├── frontend/
│   ├── src/
│   │   ├── pages/          # Сторінки
│   │   ├── components/     # Компоненти
│   │   ├── api/            # API клієнт
│   │   └── types/          # TypeScript типи
│   └── vite.config.ts
└── docker-compose.yml
```

---

## `>` ТЕСТУВАННЯ

```bash
cd backend/tests
pytest tests.py -v
```

Тести використовують in-memory SQLite та фейковий Redis — зовнішні сервіси не потрібні.

---

<div align="center">

**[ SERVICE DESK ]** — система технічної підтримки

</div>